export const notifierTypeSelector = state => state.notifier.type

export const notifierContentSelector = state => state.notifier.content

export const notifierDurationSelector = state => state.notifier.duration

export const isRequestEnabledSelector = state => state.notifier.isRequestEnabled
